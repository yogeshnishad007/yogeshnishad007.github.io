import { Load } from './Styles';

const Loader = () => {
    return (
        <Load>
            <span>	&#706;</span>
            <span>	&#707;</span>
        
        </Load>
    );
};

export default Loader;
